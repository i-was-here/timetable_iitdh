- make sure python3 is installed on the pc
- create a python virtual venv named: `timetable`, use this command: ```python3 -m venv timetable```
- install required libraries using this command: `pip install -r requirements.txt`

- now you can execute the bash script: `run.sh`, in order to get the timetable as a pdf in 